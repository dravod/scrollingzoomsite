import React from 'react';
import {Component} from 'react'
import posed from 'react-pose';

export default class  extends Component.Homescreen {
    state = {  }
    const Box = posed.div({
        hidden: { opacity: 0 },
        visible: { opacity: 1 }
      });
    render() { 
        return ( 


         );
    }
}